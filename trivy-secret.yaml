# Trivy secret scanner settings (used by scripts/security-scan.sh).
# Test fixtures hold fake, token-shaped strings on purpose to test detection.
allow-rules:
  - id: test-fixtures
    description: Fake secrets in test fixtures
    path: '(^|/)testdata/'
