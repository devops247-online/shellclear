# Rule tests. "{{c*n}}" expands to c repeated n times; expected "" means no match.

- name: "deploy token"
  test: "git clone https://gitlab+deploy:gldt-{{x*20}}@gitlab.com/g/p.git"
  expected: "gldt-{{x*20}}"

- name: "too short"
  test: "gldt-short"
  expected: ""
