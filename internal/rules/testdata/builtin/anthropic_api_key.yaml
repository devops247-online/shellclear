# Rule tests. "{{c*n}}" expands to c repeated n times; expected "" means no match.

- name: "env export"
  test: "export ANTHROPIC_API_KEY=sk-ant-api03-{{a*93}}AA"
  expected: "sk-ant-api03-{{a*93}}AA"

- name: "prefix only"
  test: "echo sk-ant-"
  expected: ""
