# Rule tests. "{{c*n}}" expands to c repeated n times; expected "" means no match.

- name: "fine-grained token"
  test: "gh auth login --with-token <<< github_pat_11{{A*20}}_{{b*59}}"
  expected: "github_pat_11{{A*20}}_{{b*59}}"

- name: "too short"
  test: "github_pat_short"
  expected: ""
