# Rule tests. "{{c*n}}" expands to c repeated n times; expected "" means no match.

- name: "secret value"
  test: "app --secret S3cretValue"
  expected: "S3cretValue"

- name: "docker build secret mount"
  test: "docker build --secret id=npmrc,src=$HOME/.npmrc ."
  expected: ""
