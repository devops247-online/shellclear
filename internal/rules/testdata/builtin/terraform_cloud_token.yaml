# Rule tests. "{{c*n}}" expands to c repeated n times; expected "" means no match.

- name: "api token"
  test: "export TFE_TOKEN={{t*14}}.atlasv1.{{y*67}}"
  expected: "{{t*14}}.atlasv1.{{y*67}}"

- name: "too short"
  test: "abc.atlasv1.def"
  expected: ""
